/* Minimal product subsequence where adjacent elements are separated by
a maximum distance of K
Given an array arr[] and an integer K, the task is to find out the
minimum product of a subsequence where adjacent elements of the 
subsequence are separated by a maximum distance of K.
